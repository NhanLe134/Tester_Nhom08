{
  "name": "taphoanga-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node --no-warnings server.js",
    "start:pg": "node --no-warnings server-pg.js",
    "dev": "nodemon --exec 'node --no-warnings' server.js",
    "dev:pg": "nodemon --exec 'node --no-warnings' server-pg.js",
    "seed": "node --no-warnings db/seed.js",
    "seed:pg": "node --no-warnings db/seed-pg.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
